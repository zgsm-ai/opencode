用直接响应标记子 agent 任务完成，返回给父 agent。

当你完成了父 agent 分配的任务时使用此工具。
direct_response 应直接回答分配的任务 - 而不是总结你采取了什么操作。

把它想象成问答格式：
- 父 agent 问："这个模块做什么？"
- 你回答："这个模块处理用户认证，包括登录和令牌刷新"
- 而不是："我首先读取了文件，然后分析了导入，发现..."

direct_response 将作为你的任务结果返回给父 agent。
