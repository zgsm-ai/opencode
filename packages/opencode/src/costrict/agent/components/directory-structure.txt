## 目录结构

```
.cospec/plan/
└── changes/               # 提案 - 具体变更的内容
    └─ [change-id]/
       ├── proposal.md     # 原因、内容、影响
       └── task.md         # 更新后的实施清单
```

**说明**：
- `proposal.md`：记录变更的原因、内容和影响
- `task.md`：实施清单，记录所有待完成和已完成的任务
- `task.md` 是工作进度的实时记录，每次任务状态更新后都要及时更新
